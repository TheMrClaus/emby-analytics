# Frontend (Next.js) environment variables

# Base URL for API calls (empty means same origin)
NEXT_PUBLIC_API_BASE=

# Optional: Admin token to include in Authorization header for /admin/* requests
# If not set at build time, you can set it at runtime from the UI prompt
# or by putting it into localStorage under key 'emby_admin_token'.
NEXT_PUBLIC_ADMIN_TOKEN=

